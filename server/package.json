{
  "name": "server",
  "version": "1.0.0",
  "description": "A backend system to the Phi Internal project",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "client": "cd .. && cd client && npm start",
    "dev": "concurrently -n 'server,client' -c 'blue,green'  \"nodemon app.js\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.14",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.6",
    "path": "^0.12.7"
  }
}
